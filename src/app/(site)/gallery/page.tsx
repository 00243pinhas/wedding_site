import { PlaceholderSection } from "@/components/placeholder-section";

export default function GalleryPage() {
  return (
    <PlaceholderSection
      title="Gallery"
      note="[Photo gallery placeholder — content pending]"
    />
  );
}
