import { PlaceholderSection } from "@/components/placeholder-section";

export default function ContactPage() {
  return (
    <PlaceholderSection
      title="Contact"
      note="[Nancy & Esa contact placeholder — content pending]"
    />
  );
}
