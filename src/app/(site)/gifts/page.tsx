import { PlaceholderSection } from "@/components/placeholder-section";

export default function GiftsPage() {
  return (
    <PlaceholderSection
      title="Gifts"
      note="[How to give placeholder — content pending, TBD]"
    />
  );
}
