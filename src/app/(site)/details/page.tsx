import { PlaceholderSection } from "@/components/placeholder-section";

export default function DetailsPage() {
  return (
    <PlaceholderSection
      title="Details"
      note="[Venue, schedule, and dress code placeholder — Black Tie Optional. Content pending]"
    />
  );
}
