import { PlaceholderSection } from "@/components/placeholder-section";

export default function RsvpPage() {
  return (
    <PlaceholderSection
      title="RSVP"
      note="[RSVP form placeholder — writes to Supabase `rsvps` table, content/form pending]"
    />
  );
}
